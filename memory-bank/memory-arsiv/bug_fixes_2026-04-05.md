# Task Log: Bug Fixes (#30, #31, #32)

## Status
- **🔴 In Progress**

## Objective
Finalizing Project Cleanup And Bugfixes by resolving tab management, initial state, and zoom issues.

## Tasks
- [ ] **#30: Tab Creation Failure**
    - Fix container ID in `document-coordinator.ts` (change `imageTabs` to `imageTabBar`).
- [ ] **#31: Initial State & Dialog Language**
    - Create initial 500x500 transparent canvas on startup.
    - Localize "New Image" modal in `index.html` using `data-i18n`.
    - Update `i18n.js` with missing keys.
    - Show "New Image" dialog automatically when all documents are closed.
- [ ] **#32: Zoom Slider & Button**
    - Connect `zoomSlider` to `applyZoom` in `document-coordinator.ts`.
    - Verify `actualSize()` button functionality.

## Progress / Decisions
- Identified that `document-coordinator.ts` was looking for `imageTabs` while `index.html` used `imageTabBar`.
- Discovered that `applyZoom` was not being called from the zoom slider, leading to non-responsive zoom.
- Planned initial document creation in `ui-coordinator.ts`.
