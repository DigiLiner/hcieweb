# Task Log #1007 – Floodfill Selection Constraint

## Objective
Restrict the Floodfill tool's operation so that it only fills areas within the current active selection mask.

## Context
The `floodFill` algorithm in `packages/tools/src/flood_fill.ts` already supports a `maskData` parameter, but it's likely being passed as `null` at the call site in `drawing_canvas.ts`.

## Plan
1.  Investigate `packages/canvas-ui/src/drawing_canvas.ts` to locate the `floodFill` call.
2.  Determine how to access the current selection mask data.
3.  Update the `floodFill` call to pass the mask data if a selection is active.
4.  Verify the fix by testing in the application.

## Log
### 2026-03-30 01:45
- Investigated `packages/canvas-ui/src/drawing_canvas.ts`.
- Found the `floodFill` call site at line 415.
- Observed that while it was already attempting to pass `maskData`, it was relying on `g.selectionMask` which might be unreliable or outdated.
- Updated the call site to fetch fresh `ImageData.data` from `g.selectionCanvas` directly for maximum reliability, as recommended by the implementation plan.
- Verified that `floodFill` in `packages/tools/src/flood_fill.ts` correctly handles this `maskData`.
- Moving to 🟡 Waiting to Confirm status.
